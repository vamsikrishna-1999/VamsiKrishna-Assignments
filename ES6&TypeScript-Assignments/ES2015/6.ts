let model="wa47df";
let deskno=23;
let Name="vamsi";
let problem="computer does not turn on. "
let ticket=function(model:string,deskno:number,Name:string,problem:string)
{
    var res1=`model:${model} deskno:${deskno} Name:${Name} problem:${problem}` 
    console.log(res1);
}
ticket(model,deskno,Name,problem);

